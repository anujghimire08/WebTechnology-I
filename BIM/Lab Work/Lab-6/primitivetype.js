const word = "anuj";
const val = 12;
const isEmpty = true;
const json = { name: "anuj" };

document.writeln(`"${word}" type - ${typeof word}`);
document.writeln(`${val} type - ${typeof val} `);
document.writeln(`${isEmpty} type - ${typeof isEmpty} `);
document.writeln(`${json} type - ${typeof JSON} `);
