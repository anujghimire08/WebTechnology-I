console.log("Hello, World!");
/*
The most common of these methods is console. log().

In a browser environment, the console. log() function is predominantly used for debugging purposes.

the console. log() function prints Hello, World! to the console and returns undefined because console. log() has no explicit return value.
*/

var greet = "Hello",
  who = "World";
console.log("%s, %s!", greet, who);
