// An easy way to get an input from a user is by using the prompt() method. SYNTAX:prompt(text, [default]);

let age = prompt("How old are you?");
console.log(age); // Prints the value inserted by the user
