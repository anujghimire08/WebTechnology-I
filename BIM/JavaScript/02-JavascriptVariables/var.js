function demo() {
  if (true) {
    var a = 5;
  }
  console.log(a); // accessed
}

/**
 * older way of defining variable
 * Can change value
 * Can be redeclared
 * Function scoped
 * visible inside whole function
 * No TDZ
 */
