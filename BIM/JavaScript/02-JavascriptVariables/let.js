function demo() {
  if (true) {
    let b = 5;
  }
  console.log(b); // error
}

/**
 * modern way of defining variable
 * Value can change
 * Cannot redeclare in same scope
 * Block scoped { }
 * recommended for changing values
 * TDZ
 */
